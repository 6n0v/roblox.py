[build-system]
requires = ["setuptools>=69", "setuptools-scm[simple]>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "arawe"
description = "A lightweight Python wrapper for the Roblox API focused on user data and economy."
readme = { file = "README.rst", content-type = "text/x-rst" }
requires-python = ">=3.8"
authors = [{ name = "6n0v" }]
classifiers = [
    "Intended Audience :: Developers",
    "Natural Language :: English",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Internet",
    "Topic :: Software Development :: Libraries",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Utilities",
    "Typing :: Typed",
]
dynamic = ["version", "dependencies"]

[project.urls]
"Issue tracker" = "https://github.com/6n0v/arawe/issues"

[tool.setuptools.dynamic]
dependencies = { file = "requirements.txt" }

[project.optional-dependencies]
test = [
    "pytest>=8,<9",
    "pytest-cov>=5,<7",
    "pytest-asyncio>=0.23,<1",
]
dev = [
    "ruff==0.12",
]


[tool.setuptools]
packages = [
    "roblox",
    "roblox.types",
]
include-package-data = true
license-files = ["LICEN[CS]E*"]

[tool.setuptools_scm]
version_file = "roblox/_version.py"
version_scheme = "no-guess-dev"
local_scheme = "node-and-date"

[tool.black]
line-length = 125
skip-string-normalization = true

[tool.ruff]
line-length = 125

[tool.ruff.format]
line-ending = "lf"
quote-style = "single"

[tool.isort]
profile = "black"
combine_as_imports = true
combine_star = true
line_length = 125

[tool.pyright]
include = [
    "roblox",
    "roblox/types",
]
exclude = [
    "**/.*",
    "**/__pycache__",
    "build",
    "dist",
    "docs",
]
reportUnnecessaryTypeIgnoreComment = "warning"
reportUnusedImport = "error"
pythonVersion = "3.14"
typeCheckingMode = "basic"

[tool.pytest.ini_options]
asyncio_mode = "strict"
asyncio_default_fixture_loop_scope = "function"
